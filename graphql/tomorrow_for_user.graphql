query TomorrowForUser($user_id: Int!) {
    dates(where: {users_to_dates: {User: {telegram_chat_id: {_eq: $user_id}}}}) {
        date
        trashTypeByTrashType {
            name
        }
    }
}
